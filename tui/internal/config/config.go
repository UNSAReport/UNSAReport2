package config

// Project configuration lives in tui/internal/project (unsareport.toml).
// This file intentionally holds no project-config types; shared constants,
// environment resolution, and credentials remain in constants.go and store.go.
